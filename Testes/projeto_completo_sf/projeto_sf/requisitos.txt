# Sistema Facil - Dependencias Python
# Instalar com: pip install -r requisitos.txt

# Conector MySQL (fallback automatico para SQLite caso indisponivel)
mysql-connector-python>=8.0.33

# Hash seguro de senhas (com fallback SHA-256 caso indisponivel)
bcrypt>=4.0.1

# Manipulacao de imagens (para upload de foto de perfil)
Pillow>=10.0.0
